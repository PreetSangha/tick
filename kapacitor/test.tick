dbrp "telegraf"."autogen"

stream
    |from()
        .measurement('cpu')
//    |eval(lambda: "cpu" == 'cpu-total')
//	.as('istotal')
    // Write the transformed data to InfluxDB
    |influxDBOut()
        .database('telegraf')
        .measurement('copy_cpu')


